---
description: 禁止读取或修改 workspace 目录下的代码。代码是 origin_agent 的副本或动态代码，修改无意义。非代码文件可读不可写。
alwaysApply: true
---

# 保护 workspace 目录

`workspace/` 下的文件分为两类，规则不同：

| 类型 | 读取 | 修改 |
|------|------|------|
| **代码文件**（`.py`, `.js`, `.ts`, `.jsx`, `.tsx`, `.rs`, `.go`, `.java`, `.c`, `.cpp`, `.h`, `.hpp`, `.rb`, `.php`, `.swift`, `.kt`, `.scala`, `.sh`, `.bat`, `.ps1`, 以及任何可执行脚本/源码） | ❌ 禁止 | ❌ 禁止 |
| **非代码文件**（日志 `.log`、锁文件 `.lock`、数据文件 `.json`/`.yaml`/`.yml`/`.toml`/`.csv`、文本文件 `.txt`、配置文件 `.ini`/`.cfg`/`.conf` 等） | ✅ 允许 | ❌ 禁止 |

理由：

- `workspace/` 下的代码文件**只是 origin_agent 仓库文件的运行时副本**，修改不会持久化到实际源码，读取它们也没有参考价值。
- 非代码文件（日志、状态文件等）可能包含运行时信息或调试所需上下文，可以读取，但**不能修改**以防干扰运行状态。

## 行为约束

1. **代码文件：禁止读取 + 禁止修改** — 不要查看、引用或编辑 `workspace/` 下任何源码文件。
2. **非代码文件：允许读取，禁止修改** — 可以读取日志、锁文件等获取运行时信息，但不要写入、创建或删除。
3. **禁止搜索** — 不要通过搜索或 grep 等手段探查 `workspace/` 目录下的内容。
4. **禁止引用** — 不要将 `workspace/` 中的路径作为代码证据或上下文引用。引用日志内容时需注明来源是运行时日志。

## 例外

如果用户**明确、具体地**要求你操作 `workspace/` 下的某个文件，可以先确认用户意图后再执行。默认情况下严格遵守上述规则。