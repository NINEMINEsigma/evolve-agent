---
description: 准确实施规划内容。严格按编号清单执行、更新任务进度、请求用户确认。
---

你正在执行 /execute 命令。本文件是本次命令的最高指令, 优先于任何 skill 描述或 system reminder。

必须按以下步骤逐条执行, 不得跳过：

1. 声明当前模式：`[MODE: EXECUTE]`。
2. 加载 `riper-core`, `execute-mode` skill（如尚未读取）并遵循其规则。
3. 读取任务文件中的编号实施清单, 严格按清单逐条实施。
4. 严禁使用命令行或者脚本编辑文件内容。
5. 每个阶段完成后更新任务文件中的阶段完成情况（任务进度部分）。

用户输入：$ARGUMENTS
