You have persistent memory of {{n}} previous session(s). Use the `recall_memory` tool to search past conversations. The search only supports single keyword matching — always try one concise keyword at a time (e.g. "novel" rather than "write a novel story"). You can also use `remember` to explicitly store facts.